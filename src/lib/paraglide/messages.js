// @ts-nocheck
/* eslint-disable */
import { languageTag } from "./runtime.js"
import * as de from "./messages/de.js"
import * as en from "./messages/en.js"
import * as es from "./messages/es.js"
import * as fr from "./messages/fr.js"
import * as it from "./messages/it.js"

/* @__NO_SIDE_EFFECTS__ */
export const invited = (params = {}, options = {}) => {
	return {
		de: de.invited,
		en: en.invited,
		es: es.invited,
		fr: fr.invited,
		it: it.invited
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const loading = (params = {}, options = {}) => {
	return {
		de: de.loading,
		en: en.loading,
		es: es.loading,
		fr: fr.loading,
		it: it.loading
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const not_found = (params = {}, options = {}) => {
	return {
		de: de.not_found,
		en: en.not_found,
		es: es.not_found,
		fr: fr.not_found,
		it: it.not_found
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const add_to_calendar = (params = {}, options = {}) => {
	return {
		de: de.add_to_calendar,
		en: en.add_to_calendar,
		es: es.add_to_calendar,
		fr: fr.add_to_calendar,
		it: it.add_to_calendar
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const can_you_come = (params = {}, options = {}) => {
	return {
		de: de.can_you_come,
		en: en.can_you_come,
		es: es.can_you_come,
		fr: fr.can_you_come,
		it: it.can_you_come
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const your_name = (params = {}, options = {}) => {
	return {
		de: de.your_name,
		en: en.your_name,
		es: es.your_name,
		fr: fr.your_name,
		it: it.your_name
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const yes = (params = {}, options = {}) => {
	return {
		de: de.yes,
		en: en.yes,
		es: es.yes,
		fr: fr.yes,
		it: it.yes
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const maybe = (params = {}, options = {}) => {
	return {
		de: de.maybe,
		en: en.maybe,
		es: es.maybe,
		fr: fr.maybe,
		it: it.maybe
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const no = (params = {}, options = {}) => {
	return {
		de: de.no,
		en: en.no,
		es: es.no,
		fr: fr.no,
		it: it.no
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const message_optional = (params = {}, options = {}) => {
	return {
		de: de.message_optional,
		en: en.message_optional,
		es: es.message_optional,
		fr: fr.message_optional,
		it: it.message_optional
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const send_rsvp = (params = {}, options = {}) => {
	return {
		de: de.send_rsvp,
		en: en.send_rsvp,
		es: es.send_rsvp,
		fr: fr.send_rsvp,
		it: it.send_rsvp
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const sending = (params = {}, options = {}) => {
	return {
		de: de.sending,
		en: en.sending,
		es: es.sending,
		fr: fr.sending,
		it: it.sending
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const thank_you = (params = {}, options = {}) => {
	return {
		de: de.thank_you,
		en: en.thank_you,
		es: es.thank_you,
		fr: fr.thank_you,
		it: it.thank_you
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const responses = (params = {}, options = {}) => {
	return {
		de: de.responses,
		en: en.responses,
		es: es.responses,
		fr: fr.responses,
		it: it.responses
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const created_with = (params = {}, options = {}) => {
	return {
		de: de.created_with,
		en: en.created_with,
		es: es.created_with,
		fr: fr.created_with,
		it: it.created_with
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const share_whatsapp = (params = {}, options = {}) => {
	return {
		de: de.share_whatsapp,
		en: en.share_whatsapp,
		es: es.share_whatsapp,
		fr: fr.share_whatsapp,
		it: it.share_whatsapp
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const copy_link = (params = {}, options = {}) => {
	return {
		de: de.copy_link,
		en: en.copy_link,
		es: es.copy_link,
		fr: fr.copy_link,
		it: it.copy_link
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const copied = (params = {}, options = {}) => {
	return {
		de: de.copied,
		en: en.copied,
		es: es.copied,
		fr: fr.copied,
		it: it.copied
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const upgrade_title = (params = {}, options = {}) => {
	return {
		de: de.upgrade_title,
		en: en.upgrade_title,
		es: es.upgrade_title,
		fr: fr.upgrade_title,
		it: it.upgrade_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const upgrade_text = (params = {}, options = {}) => {
	return {
		de: de.upgrade_text,
		en: en.upgrade_text,
		es: es.upgrade_text,
		fr: fr.upgrade_text,
		it: it.upgrade_text
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const upgrade_btn = (params = {}, options = {}) => {
	return {
		de: de.upgrade_btn,
		en: en.upgrade_btn,
		es: es.upgrade_btn,
		fr: fr.upgrade_btn,
		it: it.upgrade_btn
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const remind_btn = (params = {}, options = {}) => {
	return {
		de: de.remind_btn,
		en: en.remind_btn,
		es: es.remind_btn,
		fr: fr.remind_btn,
		it: it.remind_btn
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const remind_sent = (params = {}, options = {}) => {
	return {
		de: de.remind_sent,
		en: en.remind_sent,
		es: es.remind_sent,
		fr: fr.remind_sent,
		it: it.remind_sent
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const remind_sending = (params = {}, options = {}) => {
	return {
		de: de.remind_sending,
		en: en.remind_sending,
		es: es.remind_sending,
		fr: fr.remind_sending,
		it: it.remind_sending
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const email_for_reminders = (params = {}, options = {}) => {
	return {
		de: de.email_for_reminders,
		en: en.email_for_reminders,
		es: es.email_for_reminders,
		fr: fr.email_for_reminders,
		it: it.email_for_reminders
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const create_title = (params = {}, options = {}) => {
	return {
		de: de.create_title,
		en: en.create_title,
		es: es.create_title,
		fr: fr.create_title,
		it: it.create_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const event_title = (params = {}, options = {}) => {
	return {
		de: de.event_title,
		en: en.event_title,
		es: es.event_title,
		fr: fr.event_title,
		it: it.event_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const event_date = (params = {}, options = {}) => {
	return {
		de: de.event_date,
		en: en.event_date,
		es: es.event_date,
		fr: fr.event_date,
		it: it.event_date
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const event_location = (params = {}, options = {}) => {
	return {
		de: de.event_location,
		en: en.event_location,
		es: es.event_location,
		fr: fr.event_location,
		it: it.event_location
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const event_description = (params = {}, options = {}) => {
	return {
		de: de.event_description,
		en: en.event_description,
		es: es.event_description,
		fr: fr.event_description,
		it: it.event_description
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const your_email = (params = {}, options = {}) => {
	return {
		de: de.your_email,
		en: en.your_email,
		es: es.your_email,
		fr: fr.your_email,
		it: it.your_email
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const create_btn = (params = {}, options = {}) => {
	return {
		de: de.create_btn,
		en: en.create_btn,
		es: es.create_btn,
		fr: fr.create_btn,
		it: it.create_btn
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const creating = (params = {}, options = {}) => {
	return {
		de: de.creating,
		en: en.creating,
		es: es.creating,
		fr: fr.creating,
		it: it.creating
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const hero_title = (params = {}, options = {}) => {
	return {
		de: de.hero_title,
		en: en.hero_title,
		es: es.hero_title,
		fr: fr.hero_title,
		it: it.hero_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const hero_subtitle = (params = {}, options = {}) => {
	return {
		de: de.hero_subtitle,
		en: en.hero_subtitle,
		es: es.hero_subtitle,
		fr: fr.hero_subtitle,
		it: it.hero_subtitle
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const hero_cta = (params = {}, options = {}) => {
	return {
		de: de.hero_cta,
		en: en.hero_cta,
		es: es.hero_cta,
		fr: fr.hero_cta,
		it: it.hero_cta
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const hero_hint = (params = {}, options = {}) => {
	return {
		de: de.hero_hint,
		en: en.hero_hint,
		es: es.hero_hint,
		fr: fr.hero_hint,
		it: it.hero_hint
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const feature_1_title = (params = {}, options = {}) => {
	return {
		de: de.feature_1_title,
		en: en.feature_1_title,
		es: es.feature_1_title,
		fr: fr.feature_1_title,
		it: it.feature_1_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const feature_1_text = (params = {}, options = {}) => {
	return {
		de: de.feature_1_text,
		en: en.feature_1_text,
		es: es.feature_1_text,
		fr: fr.feature_1_text,
		it: it.feature_1_text
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const feature_2_title = (params = {}, options = {}) => {
	return {
		de: de.feature_2_title,
		en: en.feature_2_title,
		es: es.feature_2_title,
		fr: fr.feature_2_title,
		it: it.feature_2_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const feature_2_text = (params = {}, options = {}) => {
	return {
		de: de.feature_2_text,
		en: en.feature_2_text,
		es: es.feature_2_text,
		fr: fr.feature_2_text,
		it: it.feature_2_text
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const feature_3_title = (params = {}, options = {}) => {
	return {
		de: de.feature_3_title,
		en: en.feature_3_title,
		es: es.feature_3_title,
		fr: fr.feature_3_title,
		it: it.feature_3_title
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const feature_3_text = (params = {}, options = {}) => {
	return {
		de: de.feature_3_text,
		en: en.feature_3_text,
		es: es.feature_3_text,
		fr: fr.feature_3_text,
		it: it.feature_3_text
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const final_cta = (params = {}, options = {}) => {
	return {
		de: de.final_cta,
		en: en.final_cta,
		es: es.final_cta,
		fr: fr.final_cta,
		it: it.final_cta
	}[options.languageTag ?? languageTag()]()
}

/* @__NO_SIDE_EFFECTS__ */
export const nav_create = (params = {}, options = {}) => {
	return {
		de: de.nav_create,
		en: en.nav_create,
		es: es.nav_create,
		fr: fr.nav_create,
		it: it.nav_create
	}[options.languageTag ?? languageTag()]()
}

